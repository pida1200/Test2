# Zkopíruj na .env (lokálně nebo ~/home-mcp/.env na serveru).

HOME_MCP_HOST=127.0.0.1
HOME_MCP_PORT=8766

# Povinné: openssl rand -hex 32
HOME_MCP_AUTH_TOKEN=

# mujdum backend (na Pi s network_mode: host)
MUJDUM_API_URL=http://127.0.0.1:3001

HOME_MCP_LOG_LEVEL=info
